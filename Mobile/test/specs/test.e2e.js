const { browser } = require('@wdio/globals');

describe('Test Suite', ()=>{
    
   /*  it('Here we go', async()=>{
        console.log(`isAndroid: ${browser.isAndroid}`);
        const text0 = await $('android.widget.EditText[@index="0"]')
        const text1 = await $('android.widget.EditText[@index="1"]')
        text0.setValue('androidAppPath')
        text1.setValue('androidAppPath')
        await browser.pause(5000)
        expect(text0).toHaveText('androidAppPath')
        const newScreenBtn = await $('~New Screen')
        await newScreenBtn.click();
        const foundText = await $('~Hello Flutter')
        expect(foundText).toBeExisting()
        browser.back()
    }) */
})